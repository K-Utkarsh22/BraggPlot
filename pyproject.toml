[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "braggplot"
version = "0.2.0"
description = "XRD diffractogram peak extraction and cubic crystal structure analysis"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
